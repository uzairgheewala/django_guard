# Schema examples

Canonical sealed example documents live in `examples/store/artifacts/`. They are generated by
`scripts/seed_sample_artifacts.py` and validated by the same codec used by the store and API.
