# Relation fan-out

A generic parent-collection scenario bound to academic plan items and courses. The case contrasts per-parent lazy relation access with a bounded eager-loading variant, while retaining result-equivalence and tenant-isolation oracles. Inspect query-family repetition, workload motifs, replacement result amplification, and the persisted baseline/candidate comparison.
