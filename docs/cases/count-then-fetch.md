# Count then fetch

An advisor-roster path counts a collection and subsequently retrieves an equivalent collection. The case exercises workload episodes that span more than one query family and validates result-preserving consolidation.
