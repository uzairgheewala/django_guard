# Deep offset pagination

Course search is evaluated at increasing offsets and logical data scales. The case is intended for experiment series that expose threshold transitions rather than claiming one universal pagination cost model.
